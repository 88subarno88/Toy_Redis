[package]
name = "toy-redis"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
[[bench]]
name = "allocator_bench"
harness = false

[[bench]]
name = "hashmap_bench"
harness = false
